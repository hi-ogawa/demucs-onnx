{
  "name": "youtube-external-audio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && cd dist/extension && zip -r ../extension.zip .",
    "typecheck": "tsc --noEmit",
    "lint": "pnpm typecheck",
    "test": "vitest run",
    "test-e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
