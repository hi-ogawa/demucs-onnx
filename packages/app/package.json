{
  "name": "demucs-onnx-app",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "vp config",
    "dev": "vite --config vite.app.config.ts",
    "build": "vite build --config vite.app.config.ts",
    "typecheck": "tsc -b",
    "lint": "vp check --fix && pnpm lint-dead-code && pnpm typecheck",
    "lint-check": "vp check && pnpm lint-dead-code && pnpm typecheck",
    "lint-dead-code": "fallow dead-code --unused-exports --unused-types",
    "test-e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.1",
    "fallow": "^3.3.0",
    "onnxruntime-web": "^1.27.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.4",
    "vite-plus": "^0.2.4"
  }
}
