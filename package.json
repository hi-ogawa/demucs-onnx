{
  "name": "demucs-onnx",
  "private": true,
  "scripts": {
    "build:model": "uv run python tools/model-export/build_models.py htdemucs",
    "build:wasm": "wasm-pack build crates/wasm --target web --release",
    "dev": "pnpm -C packages/app dev",
    "separate": "cargo run --release -p demucs-cli -- separate --models data/onnx-lean",
    "test": "pnpm -C packages/app test"
  }
}
