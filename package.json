{
  "name": "demucs-onnx",
  "private": true,
  "scripts": {
    "build:wasm": "wasm-pack build crates/wasm --target web --release",
    "dev": "pnpm --filter demucs-onnx-app dev",
    "test": "pnpm --filter demucs-onnx-app test"
  }
}
